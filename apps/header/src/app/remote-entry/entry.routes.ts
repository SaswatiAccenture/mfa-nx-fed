import { Route } from '@angular/router';
import { HeaderRemoteEntryComponent } from './entry.component';

export const remoteRoutes: Route[] = [
  { path: '', component: HeaderRemoteEntryComponent },
];
